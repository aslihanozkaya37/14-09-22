@model IEnumerable<CoverType>
@{
    ViewData["Title"] = "CoverType Index";
}


<partial name="_Notification"/>
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6"> 
            <h2 class="text-primary">CoverType List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="CoverType"  asp-action="Create" class="btn btn-primary"><i class="bi bi-plus-circle"></i> &nbsp; Create a New CoverType</a>
        </div>
    </div>
</div>
<br />
<br />
<table class="table table-bordered table-striped table-primary">
    <thead>
        <tr>
            <th>CoverType Name</th>
        
        
            
            <th>Operasyonlar </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td width="50%">@item.Name</td>
               
                <td>
                    <div class="btn-group w-75" role="group">
                        <a asp-controller="CoverType" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary mx-2 btn-sm"><i class="bi bi-pencil-square"></i> Edit</a>
                        <a asp-controller="CoverType" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger mx-2 btn-sm"><i class="bi bi-trash3-fill"></i> Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>


</table>

@*<div class="row">
    @foreach (var item in Model)
        {
    <div class="col-md-4 my-3





00">
            <div class="card">
                <h5 class="card-header">KATEGORİ</h5>
                <div class="card-body">
                    <h5 class="card-title">@item.Name</h5>
                    <p class="card-text">Display Order: @item.DisplayOrder</p>
                    <div class="btn-group w-75" role="group">
                        <a asp-controller="Category" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary mx-2 btn-sm"><i class="bi bi-pencil-square"></i> Edit</a>
                        <a asp-controller="Category" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger mx-2 btn-sm"><i class="bi bi-trash3-fill"></i> Delete</a>
                    </div>
                </div>
            </div>

    </div>
    }
</div>*@


       
